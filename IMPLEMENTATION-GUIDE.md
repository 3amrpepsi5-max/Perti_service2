# 📚 دليل استخدام ملفات Backend Logic

## 📋 الملفات المُنشأة

تم إنشاء **10 ملفات** كاملة للمشروع:

### 1️⃣ Controllers (معالجات البيانات)
- ✅ `userController.js` - معالج المستخدمين
- ✅ `vendorController.js` - معالج المزودين
- ✅ `reviewController.js` - معالج التقييمات
- ✅ `orderController.js` - معالج الطلبات

### 2️⃣ Routes (المسارات)
- ✅ `routes-users.js` - مسارات المستخدمين
- ✅ `routes-vendors.js` - مسارات المزودين
- ✅ `routes-reviews.js` - مسارات التقييمات
- ✅ `routes-orders.js` - مسارات الطلبات

### 3️⃣ Middleware (الوسطاء)
- ✅ `validation.js` - التحقق من صحة البيانات
- ✅ `errorHandler.js` - معالجة الأخطاء الشاملة

---

## 🔧 كيفية دمج الملفات في المشروع

### الخطوة 1: ترتيب الملفات

```
nozha2-backend/
├── controllers/
│   ├── authController.js (موجود)
│   ├── userController.js (جديد) ⭐
│   ├── vendorController.js (جديد) ⭐
│   ├── reviewController.js (جديد) ⭐
│   └── orderController.js (جديد) ⭐
├── routes/
│   ├── auth.js (موجود)
│   ├── users.js (جديد) ⭐
│   ├── vendors.js (جديد) ⭐
│   ├── reviews.js (جديد) ⭐
│   └── orders.js (جديد) ⭐
├── middleware/
│   ├── auth.js (موجود)
│   ├── validation.js (جديد) ⭐
│   └── errorHandler.js (جديد) ⭐
└── server.js
```

### الخطوة 2: تحديث server.js

أضف الأسطر التالية في ملف `server.js`:

```javascript
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// استيراد الـ Routes
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

const authRoutes = require('./routes/auth');
const userRoutes = require('./routes/users');
const vendorRoutes = require('./routes/vendors');
const reviewRoutes = require('./routes/reviews');
const orderRoutes = require('./routes/orders');

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// استخدام الـ Routes
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

app.use('/api/auth', authRoutes);
app.use('/api/users', userRoutes);
app.use('/api/vendors', vendorRoutes);
app.use('/api/reviews', reviewRoutes);
app.use('/api/orders', orderRoutes);

// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
// استيراد معالج الأخطاء
// ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

const { errorHandler, notFound } = require('./middleware/errorHandler');

// استخدام معالج الأخطاء (يجب أن يكون في النهاية)
app.use(notFound);
app.use(errorHandler);
```

### الخطوة 3: إنشاء Models المطلوبة

تحتاج إلى إنشاء الـ Models التالية (بنفس أسلوب User.js):

```
models/
├── User.js (موجود)
├── Vendor.js (مطلوب)
├── Review.js (مطلوب)
└── Order.js (مطلوب)
```

---

## 📍 API Endpoints

### 👤 Users API
```
GET    /api/users              - الحصول على جميع المستخدمين (Admin)
GET    /api/users/stats        - إحصائيات المستخدمين (Admin)
GET    /api/users/:id          - الحصول على مستخدم محدد
PUT    /api/users/:id          - تحديث مستخدم
PUT    /api/users/:id/password - تغيير كلمة المرور
DELETE /api/users/:id          - حذف مستخدم (Admin)
PUT    /api/users/:id/activate - تفعيل حساب (Admin)
PUT    /api/users/:id/deactivate - تعطيل حساب (Admin)
```

### 🏪 Vendors API
```
GET    /api/vendors            - الحصول على جميع المزودين
GET    /api/vendors/me         - ملف المزود الخاص بي
GET    /api/vendors/nearby     - البحث عن مزودين قريبين
GET    /api/vendors/top-rated  - أفضل المزودين
GET    /api/vendors/featured   - المزودين المميزين
POST   /api/vendors            - إنشاء ملف مزود
GET    /api/vendors/:id        - الحصول على مزود محدد
PUT    /api/vendors/:id        - تحديث مزود
DELETE /api/vendors/:id        - حذف مزود
PUT    /api/vendors/:id/status - تحديث الحالة (Admin)
PUT    /api/vendors/:id/verify - التحقق من المزود (Admin)
PUT    /api/vendors/:id/featured - تمييز المزود (Admin)
GET    /api/vendors/:id/stats  - إحصائيات المزود
```

### ⭐ Reviews API
```
GET    /api/reviews            - جميع التقييمات (Admin)
POST   /api/reviews            - إضافة تقييم
GET    /api/reviews/:id        - تقييم محدد
PUT    /api/reviews/:id        - تحديث تقييم
DELETE /api/reviews/:id        - حذف تقييم
GET    /api/reviews/vendor/:id - تقييمات مزود
GET    /api/reviews/vendor/:id/stats - إحصائيات التقييمات
GET    /api/reviews/user/:id   - تقييمات مستخدم
POST   /api/reviews/:id/response - رد المزود
POST   /api/reviews/:id/flag   - الإبلاغ عن تقييم
POST   /api/reviews/:id/unflag - إلغاء الإبلاغ (Admin)
PUT    /api/reviews/:id/verify - التحقق من التقييم (Admin)
```

### 📦 Orders API
```
GET    /api/orders             - جميع الطلبات
GET    /api/orders/my-orders   - طلباتي
GET    /api/orders/stats       - إحصائيات الطلبات
GET    /api/orders/by-date     - طلبات حسب التاريخ
GET    /api/orders/vendor/:id  - طلبات مزود
POST   /api/orders             - إنشاء طلب
GET    /api/orders/:id         - طلب محدد
PUT    /api/orders/:id         - تحديث طلب
PUT    /api/orders/:id/status  - تحديث حالة الطلب
POST   /api/orders/:id/cancel  - إلغاء طلب
DELETE /api/orders/:id         - حذف طلب (Admin)
```

---

## 🔐 الصلاحيات (Authorization)

### Public (عام)
- عرض المزودين
- عرض التقييمات
- البحث عن مزودين قريبين

### Private (مستخدم مسجل)
- إنشاء/تعديل/حذف طلبات
- إضافة تقييمات
- تحديث الملف الشخصي

### Vendor (مزود)
- إدارة ملفه التجاري
- عرض طلباته
- الرد على التقييمات
- تحديث حالة الطلبات

### Admin (مدير)
- الوصول لجميع البيانات
- إدارة المستخدمين والمزودين
- التحقق من المزودين
- حذف البيانات

---

## ✅ Features المُطبقة

### 1. Validation (التحقق)
- ✅ التحقق من البريد الإلكتروني
- ✅ التحقق من رقم الهاتف المصري
- ✅ التحقق من كلمة المرور
- ✅ التحقق من بيانات المزود
- ✅ التحقق من التقييمات والطلبات

### 2. Error Handling (معالجة الأخطاء)
- ✅ معالج أخطاء شامل
- ✅ أخطاء قاعدة البيانات
- ✅ أخطاء JWT
- ✅ أخطاء 404
- ✅ رسائل خطأ بالعربية

### 3. Security (الأمان)
- ✅ التحقق من الصلاحيات
- ✅ حماية المسارات
- ✅ تنظيف البيانات
- ✅ Rate Limiting

### 4. Response Format (تنسيق موحد)
```json
{
  "success": true/false,
  "message": "رسالة بالعربية",
  "data": { ... },
  "pagination": { ... }  // للقوائم فقط
}
```

---

## 🎯 الخطوات التالية

### 1. إنشاء Models المطلوبة ⭐
```bash
models/
├── Vendor.js   - نموذج المزودين
├── Review.js   - نموذج التقييمات
└── Order.js    - نموذج الطلبات
```

### 2. تحديث server.js
- إضافة استيراد الـ Routes
- إضافة استخدام الـ Routes
- إضافة معالج الأخطاء

### 3. الاختبار
```bash
# تشغيل الخادم
npm start

# اختبار الـ API
# استخدم Postman أو Thunder Client
```

---

## 📝 ملاحظات مهمة

1. **الترتيب مهم**: معالج الأخطاء يجب أن يكون في النهاية
2. **التوثيق**: كل endpoint موثق في الكود
3. **الأمان**: جميع المسارات محمية بـ middleware مناسب
4. **العربية**: جميع الرسائل بالعربية
5. **Pagination**: مُطبق في جميع قوائم البيانات

---

## 🚀 للبدء

```bash
# 1. انسخ الملفات إلى المجلدات المناسبة
# 2. قم بإنشاء الـ Models المطلوبة
# 3. حدّث server.js
# 4. اختبر الـ API

npm start
```

---

تم بحمد الله! ✨

جميع الملفات جاهزة للاستخدام وتتبع نفس الأسلوب البرمجي الموجود في المشروع.
