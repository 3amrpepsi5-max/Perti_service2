# 🎨 دليل لوحة تحكم المزود - Vendor Dashboard

## 📋 الملفات المُنشأة

تم إنشاء **3 ملفات** كاملة للوحة تحكم المزود:

1. ✅ **vendor-dashboard.html** - الصفحة الرئيسية للوحة التحكم
2. ✅ **vendor-orders.html** - صفحة إدارة الطلبات الواردة
3. ✅ **vendor-dashboard.css** - ملف التنسيقات الخاص بلوحة التحكم

---

## 🎯 المميزات الرئيسية

### ✨ التصميم
- ✅ **Responsive Design** - يعمل على جميع الأجهزة (موبايل، تابلت، ديسكتوب)
- ✅ **RTL Support** - دعم كامل للغة العربية من اليمين لليسار
- ✅ **نفس هوية التصميم** - متطابق مع ألوان وأسلوب الموقع الرئيسي
- ✅ **أنيميشنز جذابة** - تأثيرات حركية سلسة وممتعة
- ✅ **ألوان زاهية** - تدرجات لونية مبهجة

### 🎨 الألوان المستخدمة
```css
--primary-orange: #FF6600   /* البرتقالي الأساسي */
--primary-red: #FF0000      /* الأحمر */
--primary-yellow: #FFD700   /* الذهبي */
--primary-green: #00FF00    /* الأخضر */
--primary-blue: #0066FF     /* الأزرق */
--success: #28a745          /* النجاح */
--danger: #dc3545           /* الخطر */
--warning: #ffc107          /* التحذير */
```

### 📱 الصفحات

#### 1. vendor-dashboard.html
**الصفحة الرئيسية للوحة التحكم**

المحتويات:
- 🎯 **Welcome Banner** - ترحيب شخصي بالمزود
- 📊 **4 بطاقات إحصائية**:
  - 📦 طلبات اليوم
  - ⏳ طلبات قيد الانتظار
  - 💰 إجمالي الإيرادات
  - ⭐ التقييم
- 📋 **أحدث الطلبات** - عرض آخر 3 طلبات
- 📊 **قسم الأداء** - مكان للرسوم البيانية المستقبلية

المميزات:
- زر تبديل الحالة (متاح/غير متاح)
- روابط سريعة لعرض الطلبات
- عرض الطلبات حسب الحالة (قيد الانتظار، مؤكد، جاري العمل)
- أزرار إجراءات سريعة (قبول، رفض، بدء، إنهاء)

#### 2. vendor-orders.html
**صفحة إدارة الطلبات الشاملة**

المحتويات:
- 🎯 **Page Header** - إحصائيات سريعة (إجمالي، قيد الانتظار، مكتملة)
- 🔍 **Filters Bar** - فلاتر متقدمة:
  - تبويبات حسب الحالة (الكل، قيد الانتظار، مؤكدة، جاري العمل، مكتملة، ملغية)
  - مربع البحث
  - خيارات الترتيب (الأحدث، الأقدم، السعر)
- 📦 **Orders Grid** - عرض تفصيلي للطلبات:
  - معلومات العميل (الاسم، الهاتف)
  - وصف الخدمة المطلوبة
  - العنوان والموقع
  - التاريخ والوقت
  - السعر
  - ملاحظات العميل
  - حالة الطلب
- ⚡ **Quick Actions** - أزرار الإجراءات السريعة:
  - قبول/رفض الطلب
  - الاتصال بالعميل
  - إرسال رسالة واتساب
  - بدء/إنهاء العمل
  - إلغاء الطلب
  - عرض الفاتورة
- 📄 **Pagination** - تقسيم الصفحات

---

## 🚀 كيفية الاستخدام

### الخطوة 1: ترتيب الملفات

```
project-root/
├── index.html
├── styles.css
├── api-client.js
├── vendor-dashboard.html    ⭐ (جديد)
├── vendor-orders.html        ⭐ (جديد)
├── vendor-dashboard.css      ⭐ (جديد)
└── assets/
    └── favicon.png
```

### الخطوة 2: الربط مع الملفات الموجودة

جميع الملفات الجديدة تستورد:
```html
<link rel="stylesheet" href="./styles.css">           <!-- الأساسي -->
<link rel="stylesheet" href="./vendor-dashboard.css"> <!-- الإضافي -->
<script src="./api-client.js"></script>
```

### الخطوة 3: التشغيل

فقط افتح الملفات في المتصفح:
```bash
# افتح الصفحة الرئيسية للوحة التحكم
open vendor-dashboard.html

# أو صفحة الطلبات
open vendor-orders.html
```

---

## 🔌 ربط الـ API

### الدوال الجاهزة للربط

الصفحات تحتوي على دوال JavaScript جاهزة للربط بالـ Backend:

#### في vendor-dashboard.html:
```javascript
// تحميل بيانات المزود
async function loadVendorData() {
    // TODO: استبدل بـ API call حقيقي
    const response = await fetch('/api/vendors/me', {
        headers: { 'Authorization': 'Bearer ' + getAuthToken() }
    });
    const data = await response.json();
}

// قبول طلب
function acceptOrder(orderId) {
    // TODO: API call to /api/orders/:id/status
    // body: { status: 'confirmed' }
}

// رفض طلب
function rejectOrder(orderId) {
    // TODO: API call to /api/orders/:id/cancel
}
```

#### في vendor-orders.html:
```javascript
// تحميل الطلبات
async function loadOrders(status = 'all') {
    // TODO: API call to /api/orders?vendor_id=X&status=Y
}

// فلترة الطلبات
function filterOrders(status) {
    // يعمل حالياً على Frontend
    // يمكن تحويله لـ API call
}

// البحث عن طلبات
function searchOrders() {
    // TODO: API call to /api/orders?search=...
}
```

### مثال ربط كامل:

```javascript
// في أعلى الملف
const API_BASE = 'http://localhost:5000/api';

// دالة قبول الطلب
async function acceptOrder(orderId) {
    try {
        const response = await fetch(`${API_BASE}/orders/${orderId}/status`, {
            method: 'PUT',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': 'Bearer ' + getAuthToken()
            },
            body: JSON.stringify({
                status: 'confirmed',
                vendor_notes: 'تم القبول'
            })
        });
        
        const data = await response.json();
        
        if (data.success) {
            showNotification('تم قبول الطلب بنجاح', 'success');
            setTimeout(() => location.reload(), 1000);
        } else {
            showNotification(data.message, 'error');
        }
    } catch (error) {
        console.error('Error accepting order:', error);
        showNotification('حدث خطأ في قبول الطلب', 'error');
    }
}
```

---

## 📊 حالات الطلبات

الصفحات تدعم 5 حالات للطلبات:

| الحالة | الكود | اللون | الأيقونة |
|--------|------|-------|----------|
| قيد الانتظار | `pending` | 🟠 برتقالي | ⏳ |
| مؤكد | `confirmed` | 🟢 أخضر | ✅ |
| جاري العمل | `in_progress` | 🔵 أزرق | 🔄 |
| مكتمل | `completed` | ⚫ رمادي | ✔️ |
| ملغي | `cancelled` | 🔴 أحمر | ❌ |

---

## 🎨 التخصيص

### تغيير الألوان:

في `vendor-dashboard.css`:
```css
/* غيّر هذه القيم حسب رغبتك */
.vendor-header {
    background: linear-gradient(135deg, #YOUR_COLOR_1, #YOUR_COLOR_2);
}

.welcome-banner {
    background: linear-gradient(135deg, #YOUR_COLOR_3, #YOUR_COLOR_4);
}
```

### إضافة رسوم بيانية:

في قسم Performance:
```html
<div class="chart-container">
    <!-- يمكن إضافة Chart.js, ApexCharts, أو أي مكتبة رسوم -->
    <canvas id="performanceChart"></canvas>
</div>
```

مثال باستخدام Chart.js:
```html
<script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
<script>
const ctx = document.getElementById('performanceChart').getContext('2d');
new Chart(ctx, {
    type: 'line',
    data: {
        labels: ['يناير', 'فبراير', 'مارس', 'أبريل', 'مايو'],
        datasets: [{
            label: 'الطلبات',
            data: [12, 19, 15, 22, 18],
            borderColor: '#FF6600',
            tension: 0.4
        }]
    }
});
</script>
```

---

## 🔔 نظام الإشعارات

الصفحات تحتوي على نظام إشعارات مدمج:

```javascript
// استخدام الإشعارات
showNotification('الرسالة هنا', 'success');  // نجاح (أخضر)
showNotification('الرسالة هنا', 'warning');  // تحذير (أصفر)
showNotification('الرسالة هنا', 'info');     // معلومات (أزرق)
showNotification('الرسالة هنا', 'danger');   // خطر (أحمر)
```

---

## 📱 التجاوب (Responsive)

الصفحات متجاوبة تماماً مع جميع الأحجام:

### Desktop (> 1024px)
- عرض كامل لجميع العناصر
- 4 أعمدة في الإحصائيات
- قائمة تنقل كاملة

### Tablet (768px - 1024px)
- عمودين في الإحصائيات
- تكيف تلقائي للمحتوى

### Mobile (< 768px)
- عمود واحد
- إخفاء النصوص الطويلة
- أيقونات فقط في القائمة
- أزرار أكبر للمس السهل

---

## ⚡ الميزات التفاعلية

### 1. تبديل الحالة (متاح/غير متاح)
```javascript
function toggleOnlineStatus() {
    // تبديل بين متاح وغير متاح
    // تحديث واجهة المستخدم
    // إرسال للـ Backend
}
```

### 2. الاتصال السريع
```javascript
function callCustomer(phone) {
    window.location.href = 'tel:' + phone;
}

function whatsappCustomer(phone) {
    window.open('https://wa.me/2' + phone, '_blank');
}
```

### 3. فلترة وبحث ديناميكي
- بحث في الوقت الفعلي
- فلترة حسب الحالة
- ترتيب حسب التاريخ أو السعر

---

## 🎯 نقاط الربط بالـ Backend

### API Endpoints المطلوبة:

```
GET    /api/vendors/me                    - بيانات المزود
GET    /api/orders?vendor_id=X            - طلبات المزود
GET    /api/orders?status=pending         - طلبات حسب الحالة
PUT    /api/orders/:id/status             - تحديث حالة الطلب
POST   /api/orders/:id/cancel             - إلغاء طلب
GET    /api/orders/stats                  - إحصائيات الطلبات
PUT    /api/vendors/me/status             - تغيير حالة (متاح/غير متاح)
```

### مثال Response من API:

```json
{
    "success": true,
    "data": {
        "vendor": {
            "id": 1,
            "business_name": "محل الصيانة السريعة",
            "rating": 4.8,
            "total_orders": 156,
            "pending_orders": 5
        },
        "orders": [
            {
                "id": 1,
                "order_number": "ORD-20240211-001",
                "customer_name": "أحمد محمود",
                "customer_phone": "01012345678",
                "service_description": "صيانة تكييف",
                "address": "شارع النزهة، برج السلام",
                "status": "pending",
                "price": 350,
                "created_at": "2024-02-11T14:30:00"
            }
        ]
    }
}
```

---

## 🎨 الأيقونات المستخدمة

جميع الأيقونات هي إيموجي نصية (لا تحتاج ملفات إضافية):

- 🏪 المحل
- 📦 الطلبات
- 👤 المستخدم
- 🚪 تسجيل الخروج
- 💰 الإيرادات
- ⭐ التقييم
- ⏳ قيد الانتظار
- ✅ مؤكد
- 🔄 جاري العمل
- 📞 اتصال
- 💬 واتساب
- 🔍 بحث
- 📊 إحصائيات

---

## ✅ جاهز للاستخدام!

الملفات جاهزة 100% ويمكن استخدامها مباشرة. فقط:

1. ✅ انسخ الملفات الثلاثة
2. ✅ ضعها في مجلد المشروع
3. ✅ افتح في المتصفح
4. ✅ ابدأ التطوير والربط بالـ Backend

---

## 📞 الدعم الفني

- 💡 الكود مكتوب بطريقة واضحة ومعلّق
- 📝 جميع الدوال موثقة
- 🎨 التصميم قابل للتخصيص بسهولة
- 🔌 جاهز للربط بالـ API

---

**تم بحمد الله! 🎉**

صفحات لوحة التحكم جاهزة للاستخدام مع تصميم احترافي ومتجاوب! ✨
